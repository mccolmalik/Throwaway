// refactor the functions below into arrow syntax=> myNumberFunction() {
  return ((2 ** 2 + 3) % 4) * 14;
}

function greet(name) {
  return `Hello, ${name}`;
}

const timeOfDayGreet = function(Dave, timeOfDay) {
  return `Hello, ${Dave}}, good ${timeOfDay}`;
};

function tripleAndHalf(one) {
  let triple = one * 3;
  return triple / 2;
}

function sumTwoNumbers(one, two) {
  let sum = one + two;
  console.log(`The sum of your numbers is ${sum}.`);
  return sum;
}
